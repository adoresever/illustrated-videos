Create a Tencent WorkBuddy custom Skill named `illustrated-videos` from the provided portable folder.

The Skill must select one of two verified content contracts before generating assets:

- `contentMode: explainer` + `assetStrategy: layered`: use the verified `paper-cut` explainer workflow. Reject a hero shot that is only one composite illustration. Require a character-free background plus genuinely independent subject and foreground assets, remove chroma when needed, and validate real alpha pixels.
- `contentMode: book-review` + `assetStrategy: scene-illustrations`: use the illustrated book-channel / book-review workflow. Allow one complete, original, text-free scene illustration per semantic section; require at least three distinct plates and add more as the completed narration needs them. Do not require alpha layers or pretend that subjects inside one plate move independently. Research title, author, editorial angle, spoiler level, facts, sources, quote policy, and visual exclusions first. Generate the approved narration as one track, let final audio determine the total duration and scene boundaries, and do not force a 30-second limit. Keep book title, author, quotations, and captions out of generated images and render them in code. Mark `textFree` and `visuallyInspected` true only after checking each plate against the research packet.

The only currently verified visual preset is `paper-cut`; do not claim that roadmap presets are implemented. Read project variables from `creative-brief.json`, compile prompts with `scripts/build-prompts.mjs`, and never hardcode a previous topic, book, palette, location, pose, or filename into reusable rules.

Support the portable provider choices already declared by the folder: image providers `codex-native`, `mcp`, `openai-api`, or compliant `file` assets; voice providers `edge-tts`, `openai`, or authorized `file` audio. Treat WeRead/微信读书, VoxCPM, faster-whisper, and HyperFrames as optional adapters, not prerequisites. When faster-whisper is present, use only its timing anchors and preserve approved caption wording. Keep the bundled Remotion renderer as the default.

Stop with an actionable message if no image-generation capability or compliant asset source is configured. Keep on-video attribution disabled unless the user explicitly requests it. Validate the selected asset contract, run `audit-project.mjs`, render H.264/AAC, run full FFmpeg/ffprobe decode QA, and inspect at least one frame per semantic scene and every transition. Store secrets only in environment variables; never place API keys in Skill files or documentation.

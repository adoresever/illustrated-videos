Create a Tencent WorkBuddy custom Skill named `illustrated-videos`.

The Skill must run a layered illustrated explainer-video workflow. Its verified v1 preset is `paper-cut`; do not claim that roadmap presets are already implemented. For `paper-cut`, reject a single composite illustration and require character-free background plates plus independently generated subject and foreground layers. Read project variables from `creative-brief.json`, compile prompts with `scripts/build-prompts.mjs`, and never hardcode a previous topic, palette, location, pose, or filename into reusable rules. Add configurable image providers (`mcp`, `openai-api`) and voice providers (`edge-tts`, `openai`, `file`). Stop with an actionable message if no image-generation capability is configured. Reuse the portable scripts and Remotion template from the provided `illustrated-videos` folder. Keep on-video attribution disabled unless the user explicitly requests it, validate alpha PNGs, run `audit-project.mjs`, and run FFmpeg QA after producing the MP4. Store secrets only in environment variables; never place API keys in Skill files or documentation.
